import React, { useState } from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const Play=({route,trackdetails})=>{
    console.log(trackdetails)
    return(
        <Text>Hii</Text>
    )
}

export default Play